# TLS-сертифікати та приватні ключі (Крок 5 — HTTPS)
# ⚠️ tls.key — приватний ключ, НІКОЛИ не комітимо в git.
# Сертифікат генерується локально командою openssl (див. README).
tls/

# Системні файли macOS
.DS_Store
